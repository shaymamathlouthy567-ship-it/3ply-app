<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>3-Ply</title>
    <meta name="theme-color" content="#000000">
    <link rel="manifest" href="manifest.json">
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <div class="container">
        <h1>The Stash.</h1>
        <p class="subtitle">Avoid the unthinkable.</p>

        <div class="input-group">
            <label>Last Restock Date</label>
            <input type="date" id="purchaseDate" required>
        </div>

        <div class="input-group">
            <label>Rolls Purchased</label>
            <input type="number" id="rollsCount" placeholder="e.g., 12" inputmode="numeric" required>
        </div>

        <div class="input-group">
            <label>Days 1 Roll Survives</label>
            <input type="number" id="daysPerRoll" placeholder="e.g., 3" inputmode="numeric" required>
        </div>

        <button id="setReminderBtn">Secure the Future</button>

        <!-- Tip Jar -->
        <a href="YOUR_TIP_LINK_HERE" class="tip-jar" target="_blank">Tip me so I don't use leaves ☕</a>
    </div>

    <!-- iOS Install Prompt (Hidden by default) -->
    <div id="iosPrompt" class="ios-prompt" style="display: none;">
        Tap the Share button <span>↑</span> below and select <strong>"Add to Home Screen"</strong> to install.
    </div>

    <script src="app.js"></script>
</body>
</html>